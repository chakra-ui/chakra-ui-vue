import { SNA } from '../../config/props.types'

const flexProps = {
  as: {
    type: String,
    default: 'div'
  },
  align: SNA,
  justify: SNA,
  wrap: SNA,
  direction: SNA,
  size: SNA
}

export default flexProps
