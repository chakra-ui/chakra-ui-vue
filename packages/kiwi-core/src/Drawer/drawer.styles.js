/**
 *let placements = {
  bottom: {
    maxWidth: '100vw',
    height: props.finalHeight,
    bottom: 0,
    left: 0,
    right: 0
  },
  top: {
    maxWidth: '100vw',
    height: props.finalHeight,
    top: 0,
    left: 0,
    right: 0
  },
  left: {
    ...(props.finalWidth && { maxWidth: props.finalWidth }),
    height: '100vh',
    left: 0,
    top: 0
  },
  right: {
    ...(props.finalWidth && { maxWidth: props.finalWidth }),
    right: 0,
    top: 0,
    height: '100vh'
  }
}
 */
